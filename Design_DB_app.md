# DB共有管理アプリ 設計メモ

> 会話で決めてきたことを一箇所に集約した「設計の地図」。
> コードではなく、何を作るか・何が決まっていて何が未定かを示すもの。
> 章末の「8. 残る設計課題」が、この次に検討していく項目。

---

## 1. 目的と対象

**対象ユーザー**: DB初心者〜中級者。SQLが何を言っているか分からない、DBで何ができるか分からない、表計算ソフト(Excel / スプレッドシート)との違いが分からない層。

**ゴール**:
- DBへの免疫を付けてもらう(触れて慣れる、怖くなくする)。
- 効率の悪い代替品(Excel / スプレッドシート)から脱却してもらい、DBでデータを管理できるようにする。

**このアプリの二面性**:
- (A) DBを学ぶ **教育ツール**
- (B) 学んだ人が実データを管理する **実用ツール**(Excel / スプレッドシートの代替)

→ **当面は (B) 実用ツールを主目的**とする。(A) 教育用の練習領域は後回し(→ 8.9)。

---

## 2. 利用形態(確定)

- **数人の固定チーム**で使う(不特定多数への公開ではない)。
- **実データをチームで共有**する。一人だけが見られるのは不可。
- **共有は「一往復モデル + 更新で反映」**:
  - 全員が同じデータを見られる。
  - 各自が操作(INSERT / UPDATE 等)を送れる。
  - 他人の変更は、画面を更新した時点(または再取得ボタン)で反映される。
  - **リアルタイム同期はしない**(スプレッドシートのように他人の編集が即座に見えることは求めない)。
- **SQL表示は付ける。ただし表示/非表示を切り替えられる**(学習中は見せ、実用時は隠せる)。
- 複数端末(スマホ・PC)から、許可があれば見られるようにする。

**代替したい対象への不満(動機)**:
- スプレッドシート: スマホでの本格操作や他アプリ連携が弱い。
- Excel: 囲い込みが強く扱いにくい。
- → スマホからでもデータを扱え、他アプリとつなげられる、開かれたデータ管理を目指す。

**なぜ一往復モデルを選んだか**:
リアルタイム同期は WebSocket と競合解決が必要で難易度が跳ね上がり、SQLの離散的な操作(INSERT一発、UPDATE一発)とも噛み合いにくい。目的(数人で議論しながら実データを管理)には一往復モデルで十分で、正しさもDBのトランザクションに任せられて素直。

---

## 3. 機能要件

**必須**:
- テーブルの中身をすべて見られる。DBの返答を確認できる。
- DBの基本操作が全てできる(CREATE / INSERT / DELETE / UPDATE / GROUP BY 等)。
  - "全部"の範囲は要確定(→ 8 の補足)。JOIN・サブクエリ・集約関数・ORDER BY 等をどこまで含めるか。
- 実行されたSQL文を見せる(表示/非表示は切り替え式)。← 学習効果の核心

**入れたい(将来)**:
- 統計処理
- DB最適化AI
- 他アプリケーションとの接続
- その他、入れたくなった機能

---

## 4. 非機能要件

- **フールプルーフ**:
  - 実DBでやってほしくない操作をさせない。
  - 処理が成功するまで別の操作をさせない。
- **セキュリティ**:
  - 許可した人間以外にDBサーバーのアドレスを見せない。
  - JSONを傍受されてもDBの中身を推測させない。
- **アクセス性**:
  - スマホ・PC等、複数端末で、許可があれば見られる。
- **同時実行の正しさ**:
  - 複数端末が同時にクエリを送っても正しさを担保する(共有するので必須)。

---

## 5. 全体構成

データは HTTP(JSON)で一往復する。層は次のとおり。

```
フロント(ブラウザ / JS)   … UIで操作 → 条件JSONを組み立て → APIへ送る / 結果とSQLを表示
      │  HTTP(JSON)
API・サーバー層(FastAPI)  … 認証・役割チェック・受け口
      │
WHERE_node                 … クライアントのJSONを検証して query_option に変換(サーバー側の防壁)
      │
DB_Manager                 … SQLを組み立てて実行(_execute)
      │
MySQL                      … データ本体
```

**設計の原則(一貫して守る)**:
- 構造は限定された語彙から組み、値は必ずプレースホルダ(%s)。
- クライアント(端末)は信頼できない入力元。検証は必ずサーバー側(WHERE_node)で行う。
- 認証情報はHTTPヘッダー、操作内容はHTTP本文、通信路の暗号化(TLS)はサーバー設定 —— 3つは別の場所。

---

## 6. 現状(実装済み・テスト済み)

- **DB_Manager**(`Operation_Database.py`): DB操作クラス。全操作が `_execute` を通り、共通の結果dict `{success, data, rowcount, lastrowid, message, error, errno}` を返す。CREATE/INSERT/UPDATE/DELETE/SELECT(条件・GROUP BY・サブクエリ・JOIN)/Drop/Truncate/Insert_Many 等を実装。テスト済み。
- **WHERE_node**(`WHERE_node.py`): 構造化JSON → query_option の生成・検証層。列名を実列と照合、演算子・論理をホワイトリストで縛る、値はプレースホルダ化、深さ制限。DB不要の単体テストで全項目パス。
- **server.py(Phase 1)**: FastAPI のAPI層。読み取り(DB一覧・テーブル一覧・列情報・select)、書き込み(insert)、破壊的操作(truncate・drop table)のエンドポイント。役割(admin/user)による認可の枠は本物。**認証は仮実装(固定トークン)**。DB不要のテスト(DB_Managerはスタブ、WHERE_nodeは本物)で全22項目パス。

→ 一往復モデルとSQL可視化は、この土台の延長で作れる。**作り直しは不要**。

---

## 7. 必須と後回しの切り分け(確定方向に基づく)

「固定チームでの実データ共有」に定まったことで、優先順位が確定した。

**必須(本質)**:
- 基本操作が全部でき、DBの返答とSQLが見える(表示切り替え式) ← 心臓部
- 認証(限られたメンバーを識別する。ただし固定の数人なので軽くてよい)
- 権限(実データを守るための役割分け。Phase 1 の枠が活きる)
- 同時実行の正しさ(共有なので必要。一往復なのでトランザクションに任せられる)

**当面は後回し・不要**:
- パスキー(固定の数人なら過剰かもしれない。もっと軽い認証で足りる可能性)
- リアルタイム同期(選ばなかった)
- 教育用サンドボックス(実用を先に)
- 統計処理 / DB最適化AI / 多数への公開

---

## 8. 残る設計課題(← 次にここを検討する)

以下は「決めるべきだがまだ未定」の項目。上から順に詰めていく想定。

### 8.1 認証方式 【決定済み】
人員は流動的(退職・参入がある)と想定。結論は次のとおり。
- **メンバー管理: 招待型**(管理者が招待を発行 → 受け取った人が自分で登録)。対になる「メンバー無効化」も必要。
- **本人確認: パスキー(WebAuthn)**。安全性を優先。サーバーには公開鍵のみ保存。検証は `py_webauthn` 等の既成ライブラリに乗る(自作しない)。
  - **※ パスキー実装は本人の強い希望。「重くても必ず実装する」で確定**(見送り・省略はしない)。順序を後にするだけで、やること自体は確定。
- **実装順序: 方式は確定するが、実装は後回し。まず心臓部(基本操作+SQL表示)を先に作る。**
  - 理由: パスキーは HTTPS + ブラウザが前提で、フロントが動いてからでないと動作確認できない。心臓部が動いて初めて「何を認証で守るか」が具体化する。
- 招待型は最初から作り込む方針(発行・失効・使い捨て・登録フロー・無効化)。ただし実装着手はフロント/HTTPSの土台ができてから。
- 認証は TLS とセットで意味を持つ(複数端末から使う段階で HTTPS 必須)。役割の枠は認証方式と独立なので、方式を変えても壊れない。

### 8.2 権限の分け方 【決定済み】
3役割(閲覧者 < 編集者 < 管理者)。実装では役割に順位を付け、「この操作は編集者以上」等と要求する。

操作を3層に分けて権限を割り当てる。

- **データ操作**(SELECT / INSERT / UPDATE / DELETE〈条件付き〉):
  - 閲覧者: SELECT系のみ(検索・GROUP BY・JOIN 等の読み取り)。
  - 編集者以上: INSERT / UPDATE / DELETE も可。
  - DELETE は条件必須(既存 `Delete_Data` の設計)。全件削除はできない → TRUNCATE(構造操作)に分離済み。
- **構造操作**(CREATE TABLE/DB, DROP, TRUNCATE, 将来の ALTER):
  - **作成(CREATE TABLE / CREATE DATABASE): 編集者以上に許す**(既存を壊さないため。「みんなで作る」)。
  - **削除・破壊(DROP, TRUNCATE): 管理者だけ**(戻せないため)。
  - ALTER 等は将来検討。原則「作成は編集者以上、破壊は管理者だけ」。
- **管理操作**(メンバーの招待・無効化・役割変更):
  - **管理者だけ**。既存メンバーでも編集者・閲覧者は不可(締め出しと管理を明確にするため)。

→ 戻せない操作(DROP / TRUNCATE / 管理操作)が管理者に集約される。Phase 1 で破壊的操作を admin 限定にした方針と一貫。

### 8.3 同時実行の正しさ 【決定済み】
共有・一往復モデルなので、**トランザクション(既存)+ 楽観ロック**で守る。悲観ロックは今回の使い方には重い(待ち・詰まりのデメリット)ため採用しない。

- **トランザクション**: 既存の `_execute`(commit / rollback)で「全部成功か全部取り消しか」を保証。中途半端な状態の露出を防ぐ。`Insert_Many` の全行ロールバックが一例。これは実装済み。
- **楽観ロック**: ロストアップデート(更新の消失)を防ぐ。各行に版(バージョン列または最終更新時刻)を持たせ、UPDATE 時に「取得時の版と現在の版が一致するときだけ更新、違えば失敗」とする。
  - 一往復モデルと噛み合う: 取得(SELECT)→ 画面で編集 → 更新(UPDATE)の間に他人が変えた可能性を、更新時の版照合で検出。
  - 弾いたときは「他の人が先に変更しました。最新を取得してください」と返す。共同作業として自然で、初心者には「同時編集で衝突する」現実の学びにもなる。
- フールプルーフ要件「処理が成功するまで別の操作をさせない」は、主にフロント側(送信中は次操作をブロック)で実現。楽観ロック(サーバー側の整合性担保)と二重に守る。
- 実装メモ: 楽観ロックには各テーブルへの版列と、`Update_Data` の UPDATE 条件への組み込みが必要 → 実装フェーズで具体化。当面同時編集が稀なら、トランザクションだけで始めて後から楽観ロックを足す段階分けも可。

### 8.4 SQL可視化の見せ方 【決定済み】
**案B: 値を埋め込んだ見やすい表示用SQLを見せる**(初心者向け)。
実行されたSQLを結果dictに含め、フロントが表示/非表示を切り替える(サーバーは常に返す)。

- 結果dictに `sql`(表示用SQL文字列)を足す。表示のオン/オフはフロントが選ぶ(応答は常に含める)。
- 表示例: `SELECT * FROM members WHERE age >= 20`(初心者が一目で分かる完成形)。

**⚠ 絶対条件: 「表示用SQL」と「実行するSQL」を厳格に分ける。**
- 実行は従来どおり **プレースホルダ + 値**(安全な分離形)で行う。`SELECT ... WHERE age >= %s` + `[20]`。
- 表示用SQLは、表示のためだけに値を整形して %s に差し込んだ **別物**。実行には一切使わない。
- 値を埋め込んだ文字列を実行に使うと、これまでのインジェクション対策が台無しになる(フールプルーフの肝)。

実装メモ:
- 表示用の値整形は型に応じて正確に行う(文字列は引用符で囲む・数値はそのまま・NULL は `NULL`)。整形を誤ると表示が実挙動とズレて誤解を生む。
- `_execute` か各メソッドで、組み立てたテンプレートと params から表示用SQLを生成して結果dictに載せる。実行経路とは分離する。
- 将来: 案A の要素(プレースホルダの姿+なぜ分離するかの説明)を詳細ビューとして足し、案C(切り替え)に育てられる。

### 8.5 フロントの配信方式 【決定済み】
**FastAPI 兼用(StaticFiles)**。フロント(HTML/CSS/JS)を Phase 1 の FastAPI から配信し、API と同一オリジンにする。

- 理由: 同一オリジンなので CORS 制約に引っかからない。将来のパスキー(RP ID = オリジン紐づけ)ともシンプルに噛み合う。数人規模なら配信を別立てする性能上の必要もない。
- 本番(Phase 5)でも、FastAPI の前に nginx / Caddy を TLS 終端として置き、内側は同一オリジンのまま、という構成にできる。案1で始めても困らない。
- **GitHub Pages は使えない**: 静的ファイル配信専用で、サーバー側で Python を動かせない。DB操作アプリはバックエンド(Python が MySQL を操作)が必須なので不可。フロントだけ GitHub Pages に置く道もあるが、CORS とパスキーのオリジン問題が増え、かつ「Python を動かすサーバーの確保」という一番重い課題は残るため、利点が薄い。
- 補足: 「Python を動かすサーバーをどこに置くか」(クラウドVM / PaaS / 自宅マシン等)は公開段階(8.8 / Phase 5)の宿題。ローカル開発中は自分のPCで uvicorn を動かせば十分。

### 8.6 エラーメッセージの初心者向け翻訳 【決定済み】
**入れる**(初心者の免疫づけに直結。失敗を学びに変える)。**置き場所は API層**で翻訳し、生の情報(errno・元メッセージ)も残す。

- 分担: `DB_Manager` は errno と生メッセージを返す(今のまま=純粋に保つ)。翻訳は「初心者に見せる」用途固有の関心なので、利用者に近い API層(`server.py`)で行う。翻訳は差し替え・拡張しやすくなる。
- 応答には「初心者向けメッセージ」と「元の errno・生メッセージ」の両方を含め、見せ方はフロントで選ぶ(8.4 と同じ発想)。
- 対応範囲: MySQL のエラーは膨大なので、初心者が踏みやすいものに絞って始める。既存テストで扱った代表例が入口 —— 重複キー(1062)、存在しないテーブル(1146)、DB/テーブル重複(1007/1050)、構文エラー(1064)、NOT NULL 違反、外部キー制約違反 等。errno → 説明文の対応表を用意し、少しずつ育てる。
- 実装順: 心臓部が動いてから、実際に初心者が踏むエラーを見ながら訳文を整える方が的を射る。方針は決定、実装は心臓部の後。

### 8.7 他アプリケーションとの接続 【方向決定・将来機能】
主な方向は**エクスポート/API(このアプリのデータを他で使う)**。ただし段階を踏む。

- **接続の向き**: 今回の主目的は「外へ出す/外から使わせる」。「外から入れる(インポート)」は当面の主眼ではない。
- **段階**:
  1. **CSVエクスポートを先**に(データを書き出して他ツールで開く)。外部にAPIの口を開かず、利用者が自分で書き出すだけなので、新たなセキュリティの口を開けずに「他で使う」を部分的に満たせる。Excel/スプレッドシートの弱点(連携)への直接的な回答。
  2. **本格的なAPI公開(外部プログラムが直接叩く)はさらに先**の将来機能。
- **API公開が持ち込む論点(将来向けに記録)**:
  - Phase 1 のAPIは既に「認証付きでJSONを返す」素地を持つ。API公開はその延長で、まったくの新規ではない。これはExcel/スプレッドシートに対する構造的な強み。
  - ただしプログラムはパスキー(生体認証)を使えない。**プログラム向けの認証(APIキー/アクセストークン)**が別途必要。キーごとの権限・失効・管理の設計を伴う。
  - セキュリティ要件(アドレスを見せない・JSON傍受でDB中身を推測させない)と緊張関係。開く以上、開き方を誤ると流出の口になる。だから心臓部・人間向け認証(8.1〜8.6)が固まった後に扱う。
- インポート(CSV取り込み)を将来やる場合は、取り込み前の検証(列数・型)が必須。`Insert_Many` の「全行を事前検証してから入れる」設計の延長で守る(外部データは信頼できない入力として扱う)。

### 8.8 実データの安全性(バックアップ等) 【方針決定・公開段階の宿題】
これまでのフールプルーフは「操作の安全」。8.8 は別の「データそのものの安全」= **失わないこと(バックアップ)** と **漏らさないこと(アクセス制限・通信保護)**。実データを載せる以上、必須。

- **バックアップ(失わない)**: 機器故障・誤操作・管理者の誤削除に備え、定期バックアップと復元手段を持つ(MySQL のダンプ等)。権限設計(8.2)で DROP/TRUNCATE を管理者に絞りリスク入口は狭めてあるが、故障・誤操作はバックアップでしか備えられない。アプリのコードでなくサーバー運用の話。
- **TLS/HTTPS(漏らさない)**: セキュリティ要件「JSON傍受でDB中身を推測させない」= TLS そのもの。**TLS が無ければこの要件は満たせない**。8.1(パスキーも HTTPS 必須)とも重なる。→ **本番では TLS は必須要件**(選択肢ではない)。
- 「アドレスを見せない」: 認証(8.1)で許可者以外を入れない + サーバーの公開範囲を絞る(ファイアウォール等)。
- サーバーの堅牢化(SSH 鍵認証・パスワードログイン無効・開放ポート最小化 等)も実データを預かる責任の中核。「動けばよい」で済ませない。
- **実装・設定は公開段階(Phase 5 前後)の宿題**。サーバーをどこに置くか(8.5 の補足)が決まってから具体化。ローカル開発中は不要。TLS は 8.1 と 8.8 の両方から要求される、と明記しておく。

### 8.9 個人専用モード(旧: 教育用サンドボックス) 【方針決定・将来課題】
当初「練習用サンドボックス」として挙げた課題を、**個人専用モード**として再定義。アプリに2モードを持たせる。

- **共有モード**(これまで設計してきた本体): 数人のチームで実データを共有。認証・役割あり。API提供もありうる。
- **個人専用モード**: 一人だけがアクセスする隔離されたDB領域。**共有もAPI提供もしない**。ここが自然に「安全な砂場」になり、練習の場も、個人的な実データ管理の場も兼ねる。

**利点**:
- サンドボックスを特別扱いせずに済む。「個人領域」という一つの仕組みが、練習も個人実用も兼ねる。練習用の作り込みが不要。
- セキュリティが単純。共有もAPI提供もしないので、「他人に見せない」「傍受対策」といった共有特有の複雑さがそもそも発生しない(守る相手と範囲が明確)。
- SQL可視化(8.4)は個人モード(練習)で真価を発揮。共有=隠せる、個人(練習)=常に見せて学ぶ、と領域で見せ方を変えられる。

**「作れる」の意味**: 利用者が自分専用のDBを作成できる(`DB_Manager.Create_DataBase` の延長)。個人領域 = その利用者だけがアクセスできる専用DB。

**設計課題(将来詰める)**:
- 共有モードのDBと個人モードのDBの区別・分離(誰の個人領域かの管理)。
- 同じ利用者が両モードを持てるか。持てるなら画面での切り替え。**両者が混ざらないこと**(個人領域の操作がチームのデータに及ばない、逆も)が新しいフールプルーフ要件。旧「練習が実データに及ばない」の再定義。
- 個人モードでも認証(8.1 パスキー)は必要。「一人だけ入れる」を保証するのが認証。共有しない=認証不要、ではない。

**順序**: 将来課題として記録。**まず共有モードの心臓部を先に作る**。個人モードは「領域の分離」「モード切り替え」という共通の仕組みの上に乗るので、共有側のDB操作・認証・領域管理が固まってから応用として足す方がきれい。

### 補足: 「基本操作が全部できる」の範囲確定
CREATE/INSERT/DELETE/UPDATE/GROUP BY に加え、JOIN・サブクエリ・集約関数(COUNT/SUM/AVG)・ORDER BY・ALTER TABLE 等をどこまで"基本"に含めるか。教育・実用の両面から、目玉になる機能(JOIN・GROUP BY など、表計算では難しくDBの価値が出る部分)を意識して決める。

---

## 9. ビルド順(改訂版ロードマップ)

目的が「固定チームの実データ管理」に定まったので、パスキー前提だった当初案を改訂する。

- **Phase 1(完了)**: API + 役割の骨組み。認証は仮。一往復の往復がローカルで動く。
- **Phase 2(次)**: フロントの心臓部。ブラウザでテーブルを見て、基本操作を送り、結果とSQL(切り替え式)を表示する。まず単純な一画面から。
- **Phase 3**: 認証を入れ、Phase 1 の仮認証を置き換える。**招待型 + パスキー(WebAuthn)**。招待の発行・使い捨て・失効、招待からの登録(パスキー登録)、メンバー無効化。権限(役割)を具体化。HTTPS が前提になる。
- **Phase 4**: 同時実行の正しさ(トランザクション/楽観ロック)を詰める。共有時の競合を安全にする。
- **Phase 5**: 複数端末からの利用とHTTPS化(localhost を越える段階)。TLS。
- **将来**: 他アプリ接続、教育用サンドボックス、統計・最適化AI。

> 注: Phase の番号は当初の「API→フロント→パスキー→公開」から組み替えている。パスキーは必須ではなくなり、認証は固定チーム向けに軽くできる見込み。